[project]
name = "let-it-rip"
version = "0.1.0"
description = "Diffusion tutorial in jax"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "pre-commit>=4.6.0",
    "pyrefly>=1.0.0",
    "ruff>=0.15.15",
]
